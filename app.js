let cart=JSON.parse(localStorage.getItem('movixia_cart')||'[]');const euro=n=>new Intl.NumberFormat('es-ES',{style:'currency',currency:'EUR'}).format(n);function card(p){let off=p.stock<=0||p.price<=0;return `<article class="product"><div class="photo">${p.image?`<img src="${p.image}" alt="${p.name}">`:'📱'}</div><span class="badge">${p.condition}</span><h3>${p.name}</h3><div class="spec">${p.specs}</div><div class="price">${p.price>0?euro(p.price):'Precio por configurar'}</div><div class="spec">${p.stock>0?p.stock+' unidades disponibles':'Sin stock configurado'}</div><button class="btn" ${off?'disabled':''} onclick="add(${p.id})">${off?'No disponible':'Añadir al carrito'}</button></article>`}function render(){let q=(document.getElementById('search').value||'').toLowerCase(),c=document.getElementById('condition').value;document.getElementById('products').innerHTML=PRODUCTS.filter(p=>(!q||(p.name+p.specs).toLowerCase().includes(q))&&(!c||p.condition===c)).map(card).join('')||'<p>No hay productos.</p>'}function add(id){let p=PRODUCTS.find(x=>x.id===id),i=cart.find(x=>x.id===id);if(i){if(i.qty<p.stock)i.qty++}else cart.push({id,qty:1});save();openCart()}function save(){localStorage.setItem('movixia_cart',JSON.stringify(cart));drawCart()}function drawCart(){let el=document.getElementById('cartItems'),total=0;document.getElementById('cartCount').textContent=cart.reduce((a,x)=>a+x.qty,0);el.innerHTML=cart.map(i=>{let p=PRODUCTS.find(x=>x.id===i.id);total+=p.price*i.qty;return `<div class="line"><div class="mini">📱</div><div><b>${p.name}</b><br><small>${euro(p.price)}</small><div class="qty"><button onclick="qty(${p.id},-1)">−</button>${i.qty}<button onclick="qty(${p.id},1)">+</button></div></div><button onclick="remove(${p.id})">✕</button></div>`}).join('')||'<p>Tu carrito está vacío.</p>';document.getElementById('cartTotal').textContent=euro(total)}function qty(id,d){let i=cart.find(x=>x.id===id),p=PRODUCTS.find(x=>x.id===id);i.qty+=d;if(i.qty<=0)cart=cart.filter(x=>x.id!==id);else if(i.qty>p.stock)i.qty=p.stock;save()}function remove(id){cart=cart.filter(x=>x.id!==id);save()}function openCart(){document.getElementById('drawer').classList.add('open');document.getElementById('overlay').classList.remove('hidden')}function closeCart(){document.getElementById('drawer').classList.remove('open');document.getElementById('overlay').classList.add('hidden')}function checkout(){if(!cart.length)return alert('El carrito está vacío.');if(cart.length===1){let p=PRODUCTS.find(x=>x.id===cart[0].id);if(p.paymentLink)return window.open(p.paymentLink,'_blank')}alert('El pago se activará cuando añadas los enlaces de pago a tus productos.')}document.getElementById('wa').href='https://wa.me/'+SHOP.whatsapp;document.getElementById('featured').innerHTML=PRODUCTS.filter(p=>p.featured).map(card).join('');render();drawCart();